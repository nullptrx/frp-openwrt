name: Build OpenWrt

on:
  push:
    paths:
      - frp.ver
  workflow_dispatch:
  repository_dispatch:
    types: [trigger-build]

permissions:
  contents: write

jobs:
  prepare_release:
    name: Prepare release
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get version
        id: get_version
        run: |
          version=$(cat frp.ver)
          echo "FRP_VERSION=$version" >> $GITHUB_ENV
          echo "FRP Version: $version"

      - name: Create release first
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ env.FRP_VERSION }}
          prerelease: false
          update_existing_release: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  cleanup_release_assets:
    name: Cleanup existing release assets
    needs: prepare_release
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get version
        id: get_version
        run: |
          version=$(cat frp.ver)
          echo "FRP_VERSION=$version" >> $GITHUB_ENV
          echo "FRP Version: $version"

      - name: Delete existing assets for current release
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const tag = `v${process.env.FRP_VERSION}`;
            const { owner, repo } = context.repo;
            const release = await github.rest.repos.getReleaseByTag({
              owner,
              repo,
              tag
            });

            const assets = release.data.assets || [];
            core.info(`Found ${assets.length} existing assets on ${tag}`);

            for (const asset of assets) {
              core.info(`Deleting asset: ${asset.name} (${asset.id})`);
              await github.rest.repos.deleteReleaseAsset({
                owner,
                repo,
                asset_id: asset.id
              });
            }

  build_luci:
    name: Build luci (${{ matrix.pkg_ext }}, ${{ matrix.sdk_ver }})
    needs: cleanup_release_assets
    runs-on: ubuntu-22.04
    env:
      SDK_DIR: ${{ github.workspace }}/../sdk
      RELEASE_DIR: ${{ github.workspace }}/release
    strategy:
      fail-fast: false
      matrix:
        include:
          # IPK: OpenWrt 24.10.5
          - pkg_ext: "ipk"
            sdk_ver: "24.10.5"
            luci_ver: "24.10"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/x86/64/openwrt-sdk-24.10.5-x86-64_gcc-13.3.0_musl.Linux-x86_64.tar.zst"

          # APK: OpenWrt snapshots
          - pkg_ext: "apk"
            sdk_ver: "snapshot"
            luci_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/x86/64/openwrt-sdk-x86-64_gcc-14.3.0_musl.Linux-x86_64.tar.zst"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get version
        id: get_version
        run: |
          version=$(cat frp.ver)
          echo "FRP_VERSION=$version" >> $GITHUB_ENV
          echo "FRP Version: $version"

      - name: Install build tools
        run: |
          sudo -E rm -rf /usr/share/dotnet /etc/mysql /etc/php /etc/apt/sources.list.d /usr/local/lib/android
          echo "Installing build tools..."
          sudo -E apt-get -qq update
          sudo -E apt-get -qq install build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses-dev libssl-dev python3-distutils python3-setuptools rsync swig unzip zlib1g-dev file wget xz-utils zstd gettext-base
          sudo -E apt-get -qq autoremove --purge
          sudo -E apt-get -qq clean

      - name: Cache OpenWrt SDK
        id: cache-sdk
        uses: actions/cache@v4
        with:
          path: cache-sdk
          key: openwrt-sdk-${{ matrix.sdk_ver }}-amd64-${{ matrix.pkg_ext }}

      - name: Download SDK
        if: steps.cache-sdk.outputs.cache-hit != 'true'
        run: |
          mkdir -p cache-sdk
          cd cache-sdk
          wget "${{ matrix.sdk_url }}"
          cd ..

      - name: Extract SDK
        run: |
          rm -rf "$SDK_DIR"
          mkdir -p "$SDK_DIR"
          file_name=$(basename "${{ matrix.sdk_url }}")
          tarball_path="$GITHUB_WORKSPACE/cache-sdk/$file_name"
          if [[ "$file_name" == *.zst ]]; then
            tar --zstd -xf "$tarball_path" -C "$SDK_DIR" --strip-components=1
          else
            tar -xf "$tarball_path" -C "$SDK_DIR" --strip-components=1
          fi

      - name: Update feeds & install package
        run: |
          cd "$SDK_DIR"

          cp -f feeds.conf.default feeds.conf
          tmp="$(mktemp)"
          {
            echo "src-link frp $GITHUB_WORKSPACE"
            cat feeds.conf
          } > "$tmp"
          mv "$tmp" feeds.conf

          sed -i \
            -e 's|https://git.openwrt.org/openwrt/|https://github.com/openwrt/|g' \
            -e 's|https://git.openwrt.org/feed/|https://github.com/openwrt/|g' \
            -e 's|https://git.openwrt.org/project/|https://github.com/openwrt/|g' \
            feeds.conf

          sed -i \
            -e '/^src-git routing[[:space:]]/d' \
            -e '/^src-git telephony[[:space:]]/d' \
            -e '/^src-git video[[:space:]]/d' \
            feeds.conf

          ./scripts/feeds update -a

          echo "CONFIG_PACKAGE_luci-app-frpc=m" > .config
          echo "CONFIG_PACKAGE_luci-app-frps=m" >> .config
          echo "CONFIG_PACKAGE_luci-i18n-frpc-zh-cn=m" >> .config
          echo "CONFIG_PACKAGE_luci-i18n-frps-zh-cn=m" >> .config
          ./scripts/feeds install -d n luci-app-frpc
          ./scripts/feeds install -d n luci-app-frps
          make defconfig
          make package/luci-app-frpc/download -j8
          make package/luci-app-frps/download -j8

      - name: Pre-generate configuration
        run: |
          cd "$SDK_DIR"
          echo "CONFIG_ALL_NONSHARED=n" > .config
          echo "CONFIG_ALL_KMODS=n" >> .config
          echo "CONFIG_ALL=n" >> .config
          echo "CONFIG_AUTOREMOVE=n" >> .config
          echo "CONFIG_SIGNED_PACKAGES=n" >> .config
          echo "CONFIG_LUCI_LANG_zh_Hans=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-frpc=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-frps=m" >> .config
          echo "CONFIG_PACKAGE_luci-i18n-frpc-zh-cn=m" >> .config
          echo "CONFIG_PACKAGE_luci-i18n-frps-zh-cn=m" >> .config
          make defconfig

      - name: Build package
        run: |
          cd "$SDK_DIR"
          make package/luci-app-frpc/{clean,compile} -j$(nproc) V=s
          make package/luci-app-frps/{clean,compile} -j$(nproc) V=s

      - name: Collect luci packages
        run: |
          cd "$SDK_DIR"
          mkdir -p "$RELEASE_DIR/luci/${{ matrix.pkg_ext }}"
          shopt -s nullglob
          files=(
            bin/packages/*/frp/luci-app-frpc*.${{ matrix.pkg_ext }}
            bin/packages/*/frp/luci-app-frps*.${{ matrix.pkg_ext }}
            bin/packages/*/frp/luci-i18n-frpc-zh-cn*.${{ matrix.pkg_ext }}
            bin/packages/*/frp/luci-i18n-frps-zh-cn*.${{ matrix.pkg_ext }}
          )
          if [ ${#files[@]} -eq 0 ]; then
            echo "No luci packages found (*.${{ matrix.pkg_ext }})" >&2
            exit 1
          fi
          cp "${files[@]}" "$RELEASE_DIR/luci/${{ matrix.pkg_ext }}/"

      - name: Upload luci artifacts
        uses: actions/upload-artifact@v4
        with:
          name: luci-${{ matrix.luci_ver }}-${{ matrix.sdk_ver }}-${{ matrix.pkg_ext }}
          path: release/luci/${{ matrix.pkg_ext }}/

      - name: Upload release assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ env.FRP_VERSION }}
          files: release/luci/${{ matrix.pkg_ext }}/*.${{ matrix.pkg_ext }}
          prerelease: false
          update_existing_release: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build_bin:
    name: Build bin (${{ matrix.pkg_ext }}, ${{ matrix.platform }})
    needs: cleanup_release_assets
    runs-on: ubuntu-22.04
    env:
      SDK_DIR: ${{ github.workspace }}/../sdk
      RELEASE_DIR: ${{ github.workspace }}/release
    strategy:
      fail-fast: false
      matrix:
        include:
          # --------------------
          # IPK (OpenWrt 24.10.5)
          # --------------------
          - pkg_ext: "ipk"
            platform: amd64
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/x86/64/openwrt-sdk-24.10.5-x86-64_gcc-13.3.0_musl.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: aarch64_generic
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/rockchip/armv8/openwrt-sdk-24.10.5-rockchip-armv8_gcc-13.3.0_musl.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: aarch64_cortex-a53
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/mvebu/cortexa53/openwrt-sdk-24.10.5-mvebu-cortexa53_gcc-13.3.0_musl.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: aarch64_cortex-a72
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/mvebu/cortexa72/openwrt-sdk-24.10.5-mvebu-cortexa72_gcc-13.3.0_musl.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: arm_cortex-a5_vfpv4
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/at91/sama5/openwrt-sdk-24.10.5-at91-sama5_gcc-13.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: arm_cortex-a7
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/mediatek/mt7629/openwrt-sdk-24.10.5-mediatek-mt7629_gcc-13.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: arm_cortex-a7_neon-vfpv4
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/sunxi/cortexa7/openwrt-sdk-24.10.5-sunxi-cortexa7_gcc-13.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: arm_cortex-a8_vfpv3
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/sunxi/cortexa8/openwrt-sdk-24.10.5-sunxi-cortexa8_gcc-13.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: arm_cortex-a9
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/bcm53xx/generic/openwrt-sdk-24.10.5-bcm53xx-generic_gcc-13.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: arm_cortex-a9_neon
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/zynq/generic/openwrt-sdk-24.10.5-zynq-generic_gcc-13.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: arm_cortex-a9_vfpv3-d16
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/mvebu/cortexa9/openwrt-sdk-24.10.5-mvebu-cortexa9_gcc-13.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: arm_cortex-a15_neon-vfpv4
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/ipq806x/generic/openwrt-sdk-24.10.5-ipq806x-generic_gcc-13.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: mips_mips32
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/bcm53xx/generic/openwrt-sdk-24.10.5-bcm53xx-generic_gcc-13.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: mipsel_24kc
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/ramips/rt288x/openwrt-sdk-24.10.5-ramips-rt288x_gcc-13.3.0_musl.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: mipsel_74kc
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/ramips/rt3883/openwrt-sdk-24.10.5-ramips-rt3883_gcc-13.3.0_musl.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: mipsel_mips32
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/bcm47xx/generic/openwrt-sdk-24.10.5-bcm47xx-generic_gcc-13.3.0_musl.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: riscv64
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/starfive/generic/openwrt-sdk-24.10.5-starfive-generic_gcc-13.3.0_musl.Linux-x86_64.tar.zst"
          - pkg_ext: "ipk"
            platform: loongarch64
            sdk_ver: "24.10.5"
            sdk_url: "https://downloads.openwrt.org/releases/24.10.5/targets/loongarch64/generic/openwrt-sdk-24.10.5-loongarch64-generic_gcc-13.3.0_musl.Linux-x86_64.tar.zst"

          # ----------------
          # APK (snapshots)
          # ----------------
          - pkg_ext: "apk"
            platform: amd64
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/x86/64/openwrt-sdk-x86-64_gcc-14.3.0_musl.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: aarch64_generic
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/rockchip/armv8/openwrt-sdk-rockchip-armv8_gcc-14.3.0_musl.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: aarch64_cortex-a53
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/mvebu/cortexa53/openwrt-sdk-mvebu-cortexa53_gcc-14.3.0_musl.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: aarch64_cortex-a72
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/mvebu/cortexa72/openwrt-sdk-mvebu-cortexa72_gcc-14.3.0_musl.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: arm_cortex-a5_vfpv4
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/at91/sama5/openwrt-sdk-at91-sama5_gcc-14.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: arm_cortex-a7
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/mediatek/mt7629/openwrt-sdk-mediatek-mt7629_gcc-14.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: arm_cortex-a7_neon-vfpv4
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/sunxi/cortexa7/openwrt-sdk-sunxi-cortexa7_gcc-14.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: arm_cortex-a8_vfpv3
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/sunxi/cortexa8/openwrt-sdk-sunxi-cortexa8_gcc-14.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: arm_cortex-a9
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/bcm53xx/generic/openwrt-sdk-bcm53xx-generic_gcc-14.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: arm_cortex-a9_neon
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/zynq/generic/openwrt-sdk-zynq-generic_gcc-14.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: arm_cortex-a9_vfpv3-d16
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/mvebu/cortexa9/openwrt-sdk-mvebu-cortexa9_gcc-14.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: arm_cortex-a15_neon-vfpv4
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/ipq806x/generic/openwrt-sdk-ipq806x-generic_gcc-14.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: mips_mips32
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/bcm53xx/generic/openwrt-sdk-bcm53xx-generic_gcc-14.3.0_musl_eabi.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: mipsel_24kc
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/ramips/rt288x/openwrt-sdk-ramips-rt288x_gcc-14.2.0_musl.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: mipsel_74kc
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/ramips/rt3883/openwrt-sdk-ramips-rt3883_gcc-14.3.0_musl.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: mipsel_mips32
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/bcm47xx/generic/openwrt-sdk-bcm47xx-generic_gcc-14.3.0_musl.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: riscv64
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/starfive/generic/openwrt-sdk-starfive-generic_gcc-14.3.0_musl.Linux-x86_64.tar.zst"
          - pkg_ext: "apk"
            platform: loongarch64
            sdk_ver: "snapshot"
            sdk_url: "https://downloads.openwrt.org/snapshots/targets/loongarch64/generic/openwrt-sdk-loongarch64-generic_gcc-14.3.0_musl.Linux-x86_64.tar.zst"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get version
        id: get_version
        run: |
          version=$(cat frp.ver)
          echo "FRP_VERSION=$version" >> $GITHUB_ENV
          echo "FRP Version: $version"

      - name: Install build tools
        run: |
          sudo -E rm -rf /usr/share/dotnet /etc/mysql /etc/php /etc/apt/sources.list.d /usr/local/lib/android
          echo "Installing build tools..."
          sudo -E apt-get -qq update
          sudo -E apt-get -qq install build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses-dev libssl-dev python3-distutils python3-setuptools rsync swig unzip zlib1g-dev file wget xz-utils zstd gettext-base
          sudo -E apt-get -qq autoremove --purge
          sudo -E apt-get -qq clean

      - name: Cache OpenWrt SDK
        id: cache-sdk
        uses: actions/cache@v4
        with:
          path: cache-sdk
          key: openwrt-sdk-${{ matrix.sdk_ver }}-${{ matrix.platform }}-${{ matrix.pkg_ext }}

      - name: Download SDK - ${{ matrix.platform }}
        if: steps.cache-sdk.outputs.cache-hit != 'true'
        run: |
          mkdir -p cache-sdk
          cd cache-sdk
          wget "${{ matrix.sdk_url }}"
          cd ..

      - name: Extract SDK
        run: |
          rm -rf "$SDK_DIR"
          mkdir -p "$SDK_DIR"
          file_name=$(basename "${{ matrix.sdk_url }}")
          tarball_path="$GITHUB_WORKSPACE/cache-sdk/$file_name"
          if [[ "$file_name" == *.zst ]]; then
            tar --zstd -xf "$tarball_path" -C "$SDK_DIR" --strip-components=1
          else
            tar -xf "$tarball_path" -C "$SDK_DIR" --strip-components=1
          fi

      - name: Update feeds & install package
        run: |
          cd "$SDK_DIR"

          cp -f feeds.conf.default feeds.conf
          tmp="$(mktemp)"
          {
            echo "src-link frp $GITHUB_WORKSPACE"
            cat feeds.conf
          } > "$tmp"
          mv "$tmp" feeds.conf

          sed -i \
            -e 's|https://git.openwrt.org/openwrt/|https://github.com/openwrt/|g' \
            -e 's|https://git.openwrt.org/feed/|https://github.com/openwrt/|g' \
            -e 's|https://git.openwrt.org/project/|https://github.com/openwrt/|g' \
            feeds.conf

          sed -i \
            -e '/^src-git routing[[:space:]]/d' \
            -e '/^src-git telephony[[:space:]]/d' \
            -e '/^src-git video[[:space:]]/d' \
            feeds.conf

          ./scripts/feeds update -a
          echo "CONFIG_PACKAGE_frpc=m" > .config
          echo "CONFIG_PACKAGE_frps=m" >> .config
          ./scripts/feeds install -d n frpc
          ./scripts/feeds install -d n frps
          make defconfig
          make package/frpc/download -j8
          make package/frps/download -j8

      - name: Pre-generate configuration
        run: |
          cd "$SDK_DIR"
          echo "CONFIG_ALL_NONSHARED=n" > .config
          echo "CONFIG_ALL_KMODS=n" >> .config
          echo "CONFIG_ALL=n" >> .config
          echo "CONFIG_AUTOREMOVE=n" >> .config
          echo "CONFIG_SIGNED_PACKAGES=n" >> .config
          echo "CONFIG_PACKAGE_frpc=m" >> .config
          echo "CONFIG_PACKAGE_frps=m" >> .config
          make defconfig

      - name: Build package for ${{ matrix.platform }}
        run: |
          cd "$SDK_DIR"
          make package/frpc/{clean,compile} -j$(nproc) V=s
          make package/frps/{clean,compile} -j$(nproc) V=s

      - name: Collect bin packages for ${{ matrix.platform }}
        run: |
          cd "$SDK_DIR"
          out="$RELEASE_DIR/bin/${{ matrix.pkg_ext }}/${{ matrix.platform }}"
          mkdir -p "$out"
          shopt -s nullglob
          files=(bin/packages/*/frp/frpc*.${{ matrix.pkg_ext }} bin/packages/*/frp/frps*.${{ matrix.pkg_ext }})
          if [ ${#files[@]} -eq 0 ]; then
            echo "No bin packages found (*.${{ matrix.pkg_ext }})" >&2
            exit 1
          fi

          for f in "${files[@]}"; do
            b="$(basename "$f")"
            if [ "${{ matrix.pkg_ext }}" = "apk" ]; then
              new="${b%.*}_${{ matrix.platform }}.${b##*.}"
            else
              new="$b"
            fi
            cp "$f" "$out/$new"
          done

      - name: Upload bin artifacts for ${{ matrix.platform }}
        uses: actions/upload-artifact@v4
        with:
          name: bin-${{ matrix.platform }}-${{ env.FRP_VERSION }}-${{ matrix.pkg_ext }}
          path: release/bin/${{ matrix.pkg_ext }}/${{ matrix.platform }}/

      - name: Upload release assets for ${{ matrix.platform }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ env.FRP_VERSION }}
          files: release/bin/${{ matrix.pkg_ext }}/${{ matrix.platform }}/*.${{ matrix.pkg_ext }}
          prerelease: false
          update_existing_release: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
